name: Check tag name
on:
  push:
    tags:
      - test-*

jobs:
  check-tag:
    name: Build and Test
    runs-on: ubuntu-latest
    env:
      VITE_RELEASE: ${{ github.ref_name || github.sha }}
    steps:
      - run: echo $VITE_RELEASE
